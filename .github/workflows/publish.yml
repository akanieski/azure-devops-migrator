name: Build and Publish by Tag

on:
  workflow_dispatch:
    inputs:
      version_number:
        description: Version Number
        required: true
  push:
    tags:
      - "v*.*.*.*"

jobs:
  build:

    runs-on: windows-latest
    env:
      VERSION: ${{ github.event.inputs.version_number || github.ref}}
  
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.100

    - name: Install dependencies
      run: dotnet restore

    - name: Publish
      run: .\Publish.ps1 -Tag $VERSION

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
      with:
        tag_name: $VERSION
        release_name: Release $VERSION
        body: Latest Build
        draft: false
        prerelease: false
        
    - name: Upload Release Asset (x64)
      id: upload-release-asset-x64
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./AzureDevOpsMigrationUtility_x64.zip
        asset_name: AzureDevOpsMigrationUtility_x64.zip
        asset_content_type: application/zip