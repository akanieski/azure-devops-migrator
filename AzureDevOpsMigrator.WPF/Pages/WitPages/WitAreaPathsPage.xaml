<Page x:Class="AzureDevOpsMigrator.WPF.Pages.WitPages.WitAreaPathsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AzureDevOpsMigrator.WPF.Pages.WitPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WitAreaPathsPage">

    <Grid Margin="0,20,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="0" Grid.Column="0">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Text="Migrate Area Paths" />
            <Button Margin="20,0,0,0" Foreground="DodgerBlue" Background="Transparent" BorderThickness="0">
            </Button>
        </StackPanel>
        <CheckBox Grid.Column="1" Grid.Row="0" VerticalAlignment="Center"
                        IsChecked="{Binding Model.Execution.AreaPathMigratorEnabled, Mode=TwoWay}" />
    </Grid>
</Page>
